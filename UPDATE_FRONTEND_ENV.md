# 🔧 Update Frontend Configuration

## ✅ Backend is Live!

Your backend is now running at: **https://biseda-ai.onrender.com**

---

## 🎯 Update Frontend .env File

### Step 1: Find or Create .env File

**In your project root** (`/Users/xhuljongashi/BISEDA.AI/`):

1. **If `.env` exists:** Open it
2. **If `.env` doesn't exist:** Create a new file named `.env`

### Step 2: Add Production Backend URL

**Add this line to your `.env` file:**

```bash
VITE_BACKEND_URL=https://biseda-ai.onrender.com
```

**If you already have `VITE_BACKEND_URL` in your `.env`:**
- Replace the old value with: `https://biseda-ai.onrender.com`

### Step 3: Save and Restart

1. **Save the `.env` file**
2. **Restart your frontend dev server** (if running)
3. **Rebuild your app** for production

---

## ✅ After Updating

Your frontend will now connect to the production backend!

---

**Update your `.env` file now!**

